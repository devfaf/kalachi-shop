import { Button } from "@mui/material"

const Auth = () => {
    return (
        <Button
            sx={{
                minWidth: "auto",
                color: "text.primary",
                whiteSpace: "nowrap",
            }}
        >
            ورود / ثبت‌نام
        </Button>
    )
}
export default Auth